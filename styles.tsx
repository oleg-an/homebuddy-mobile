import { StyleSheet } from 'react-native';

export const defaultStyles = StyleSheet.create({
  inputContainer: {
    marginBottom: 4,
  },
  inputLabel: {
    fontSize: 14,
    color: '#666',
    marginBottom: 4,
    fontWeight: '500',
  },
});
